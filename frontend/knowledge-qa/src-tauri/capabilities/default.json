{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the Knowledge QA window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
